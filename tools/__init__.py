"""Design-time tooling for the REVV1 ESP32 body module board set.

Stdlib only, deliberately. This code has to still run years from now on a
machine nobody has maintained, so it takes no dependencies it does not need.
"""
