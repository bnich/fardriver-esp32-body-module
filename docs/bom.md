# REVV1 ESP32 module — BOM

**✅ $382.75 committed** (14 lines) · ~ **$163.77 estimated** (25 lines) · **≈$547 all-in, EX-DUTY — a floor: 21 lines carry no price yet (⬜)** · ⚠️ **tariffs are counted in no total here:** $31.97 on the Mouser line (E1, 25%) and $26.09 on DigiKey order 101547984 (with $8.49 shipping and $14.68 tax, total $232.22).
📦 in hand · ⏳ ordered, not yet received · ✅ price verified · ~ approximate · ⬜ no price / to order · ◐ candidate, **unverified** · ⏸️ parked · — not used

⭐ **[`tools/netlist.py`](../tools/netlist.py) is the truth for what is FITTED; this file is the truth for what is BOUGHT.** Every fitted count and refdes below is read from the netlist. Rows marked **prototype only** serve the breadboard / perfboard stage (plan §9.3, §9.6); the four-board set (plan §9.2) does not use them. JLC assembles the four-board set from LCSC parts ([JLC assembly](#jlc-assembly--the-custom-boards)); a part already bought and in hand serves the breadboard unless its row says the board set uses it.

| # | Qty | Part | St | $ | Notes |
|---|---|---|---|---|---|
| **A1** | 1 | **ESP32-S3-DevKitC-1** `WROOM-1-N8` | 📦 | — | v1.0 board → GPIO48 excluded, GPIO38 free. Never an `R8` (65 °C). **Prototype only** — and, carrying the same module as A4, the unit for testing WiFi range from inside the real cavity before A4's variant is chosen. The board set carries the bare module (A4) |
| **A2** | 2 | Turned-pin socket strips, 2.54 mm | ⬜ | ~3 | ⚠️ Never dual-wipe — they walk out under vibration. **Prototype only** — they seat the DevKit on perfboard. On BRAIN, JLC places the module and both `MCP23017` |
| **A3** | 2 | **Bar-mount switch sets**, L + R | 📦 | ~30–50 | **Both pods in hand.** LEFT: received, harnessed and tested working 2026-09-11 — 12 wires → 8 conductors → 7 bits. It arrived wired as a **power-distribution block** (a shared +12 V feed, flash injecting onto the high-beam output), so two cuts made the switches independent contacts. RIGHT: in hand 2026-09-12, rewired onto five conductors with one shared ground — the **lighting slider** (`OFF / A / A+B`), the run/off toggle and the start button (a spare sensed input). Maps: [`inputs-bench-session.md`](inputs-bench-session.md). ⬜ Price still to log |
| **A4** | 1 | Bare **`ESP32-S3-WROOM-1U-N8`** | ⬜ | ⬜ | `U401` on BRAIN, placed by JLC. **External antenna, because the enclosure is metal (X4)** and a metal box kills a PCB antenna. The footprint also takes the PCB-antenna `-WROOM-1-N8` (same pads; the antenna keep-out stays). Needs a 2.4 GHz antenna and a U.FL (IPEX MHF1) pigtail to a bulkhead connector (⬜). ⚠️ **The bulkhead must be insulated from the box** — the box is bonded to ground, and a bulkhead touching it is a second ground bond. ⬜ Where the antenna sits is still open. 8 MB flash, no PSRAM, −40…+85 °C; ⛔ never `R8` / `R16V` (65 °C) — the variant is a thermal choice (plan D18). ⚠️ The module has **no IO33 / IO34 pads** |
| **A5** | 1 | TI **`TLV76733DGNR`** — 3.3 V / 1 A LDO, 8-HVSSOP PowerPAD | ⬜ | ⬜ | `U405`, BRAIN's 5 V → 3.3 V, placed by JLC. **Input rated to 16 V** — it rides the Cincon's 6.2 V output over-voltage clamp, which a 6 V-max LDO does not. 1 %, stable with plain ceramics, 60 °C/W. Sized for the S3's ≥500 mA supply requirement (the WiFi TX peak), not the average. ⚠️ The pad must reach copper. Its capacitors are in A7 |
| **A6** | 1 | G-Switch **`GT-USB-7010ASV`** USB-C receptacle, USB 2.0, 16-pin top-mount | ⬜ | ⬜ | `J401`, placed by JLC — native USB console and flashing, **inside the box**: a service port beside the service header (X10). −40…+85 °C. ⬜ Its 3.3 mm height is booked, not read off its drawing. CC1 / CC2 take 5.1 kΩ (A7) and a C2 array (`D408`); D± take C5, through 22 Ω at the S3 (A7). VBUS is sensed only — it never feeds the 5 V rail |
| **A7** | — | BRAIN passives no other line covers — 0805 unless noted | ⬜ | ⬜ | 10 kΩ ×4 (`MCP23017` RESET `R437` · module `EN` `R438` · `IO0` `R439` · CAN `RS` → 3.3 V `R472`, standby while CAN is parked) · 2.2 kΩ ×2 (I²C) · 5.1 kΩ ×2 (USB-C CC) · 100 kΩ ×2 + **180 kΩ** ×2 (**IN-11 divider 100 k / 180 k → 3.24 V at the expander pin** with `RUN` at 5.03 V, against V<sub>IH</sub> 2.64 V · the VBUS sense divider, the same pair → 3.21 V) · 1 kΩ ×1 (`R476`, `KEY_SENSE` series at `GPIO1`) · 22 Ω ×2 (`R473` / `R474`, USB D± series at the S3) · 470 Ω ×1 (`R475`, U0TXD series — Espressif's 499 Ω, nearest JLC Basic) · 120 Ω ×1 (CAN termination `R401`, B2) · 1 µF / 16 V ×1 (`EN` reset RC) · 10 µF / 16 V 1206 ×3 · 100 nF / 50 V ×6 (decoupling: module, regulator, both expanders, transceiver · `C437`, `KEY_SENSE` at the pin). **Not fitted**, footprints only: `U406`, a `TLV803S` supervisor on `EN`, with its 100 nF `C436` — fit it if a slow 3.3 V ramp ever shows up · `R442`, `RS` → 10 kΩ → ground: to un-park CAN, fit `R442` and remove `R472` |
| **B1** | 3 | **`SN65HVD230`** breakouts | 📦 | — | In hand ×3 — **do not order.** Characterised: 120 Ω fitted, no `Rs` exposed, 8/8 loopback at 1 Mbit — a generic replacement may be neither. **Prototype only**; BRAIN carries the bare IC (B4) |
| **B2** | 1 | 120 Ω termination | 📦 | — | On the breakout. **68 Ω joined** = the CAN pre-flight gate. On BRAIN it is a discrete 0805, `R401` (A7) |
| **B3** | 6 | 1 kΩ series | ⬜ | — | 4 on DRV's parked display block (`R426`–`R429`: the three telltale feeds and display pin-9 back-feed protection), **1206**: a display wire shorted to ground puts the 12 V feed across one, 144 mW. The headlight telltale is fed from `HL_HIGH` only, so a flash lights it · 2 on the FarDriver serial wires (`R436` TX · `R443` RX — the controller's TX level is unmeasured, M9) |
| **B4** | 1 | Bare TI **`SN65HVD230DR`**, SOIC-8 | ⬜ | ⬜ | `U404` on BRAIN, placed by JLC. The CAN hardware is fitted; the feed is parked (D19). While parked, `RS` → 10 kΩ → 3.3 V holds it in **standby** — receiver listening, driver off; `Vref` stays open. Support passives: A7 |
| **C1** | 11 | **1 kΩ** pull-up + 1 kΩ + 100 nF | ⬜ | ~5 | **11 class-A networks** = 22 × 1 kΩ + 11 × 100 nF: turn L / R · horn · low · high · flash · hazard (left pod) · running · headlight (right-pod slider) · boost button (a 2-pin dry contact on `J404` pins 6–7) · start button (a spare sensed input). ⚠️ **1 kΩ, not 4.7 kΩ** — wetting current. The brake inputs IN-05/06 take G3's 10 kΩ instead. The board set adds 13 more on `J409`'s spare inputs, which JLC places |
| **C2** | 20 | **5 V quad TVS**, SC-74 — board set: onsemi **`SMS05T1G`** (JLC places it) · breadboard: Nexperia **`PESD5V0S4UD`** (in hand) | 📦 | **8.54** | ✅ `PESD5V0S4UD` paid 09-10, **received 09-18** (20 @ $0.427, `PESD5V0S4UD,115`, DK 1727-3928-1-ND) — the breadboard's. LCSC has no `PESD5V0S4UD`, so the board set carries the `SMS05T1G`, same pinout (pads 1 / 3 / 4 / 6 cathodes, 2 / 5 anodes). **10 fitted:** BRAIN `D401` / `D402` left pod · `D403` right pod · `D404` FarDriver serial + the boost button · `D408` USB-C CC1 / CC2 · `D410`–`D413` J409's spare inputs · DRV `D405` (CAN at the parked display connector — ⚠️ ~300 pF per line is too heavy for CAN, acceptable only while the display is parked). `SMS05T1G`: V<sub>RWM</sub> 5 V · V<sub>BR</sub> 6.0 V min · 9.8 V @ 5 A · I<sub>R</sub> ≤ 20 µA · ~300 pF per line. ⛔ **Correct ONLY on lines that stay ≤5 V.** On a 12 V-class line — or a brake-lever node, which idles at ~11.4 V — it conducts continuously: a dead short across the channel it protects, and on the levers a stop lamp held permanently on. The levers take C4; the 12 V outputs, D10. ⛔ **Not on USB D±** either — C5. The right pod's `RUN` node idles at 5.03 V (5.3 V with `ACC+` at its 5.4 V top): at V<sub>RWM</sub>, and at least 0.7 V under V<sub>BR</sub> min. `PESD5V0S4UD`: V<sub>RWM</sub> 5 V · V<sub>BR</sub> 6.4–7.2 V · V<sub>CL</sub> 8 V @ 1 A / 13 V @ 20 A · 165–220 pF per line · **unidirectional ×4 lines** · common anode. ⛔ **SOT457 / SC-74, 6-pin 0.95 mm — not SOIC-8** (prototype adapter: X6) |
| **C3** | 4 | **`MCP23017`** I²C expander — board set: **`MCP23017T-E/SS`**, SSOP-28 (JLC places it) · breadboard: `MCP23017-E/SP`, DIP-28 (in hand) | 📦 | **6.76** | ✅ DIP-28s paid 09-10, **received 09-18** (4 @ $1.69, DK MCP23017-E/SP-ND) — the breadboard's. **2 fitted on BRAIN.** #1 (`U402`, 0x20) = every bar input and sense bit — **all 14 input-capable bits used.** #2 (`U403`, 0x21) = `ACC_SENSE` on `GPA0`; its other **13 input-capable bits go to `J409`**, a 2 × 8 header footprint left unfitted for controls added later, each class-A conditioned on the board. ⛔ **GPA7 and GPB7 are OUTPUT-ONLY** (DS20001952 rev D) — never land an input on them; a 16-bit expander offers 14 inputs. Lighting is full native (plan D16), so no lamp sits behind I²C. RESET and A0–A2 must be externally biased (A7). Both expanders share one RESET pull-up and no GPIO can reset them: power-cycle to recover. I²C part, not the SPI `MCP23S17` |
| **C4** | 5 | onsemi **`SMS15T1G`** quad TVS, SC-74 | ⬜ | ~3.15 | **3 fitted + 2 DNP**; JLC places the fitted ones. **Requirement:** 4-line unidirectional array, **V<sub>RWM</sub> 15 V** (V<sub>BR</sub> 16.7–18.5 V), **the same SC-74 pinout as C2** (pads 1 / 3 / 4 / 6 cathodes, 2 / 5 anodes), clamping **24.0 V @ 5 A / 29.0 V @ 12 A — under the `AO3400A`'s 30 V.** ⛔ **Not `PESD15VS4UD`** — obsolete. It goes on the brake-lever nodes (idle ~11.4 V), on `BL` / `ACC+`, and on the boost output (its pull-up is unmeasured). Fitted: DRV `D313` (J306, the levers) · `D316` (J309, `BL` / `ACC+`) · BRAIN `D407` (J404, `BOOST_OUT`). DNP with the parked display (D19): `D317` (J310) + `D406` (J405), populated when the display is un-parked. The switched 12 V lamp and load outputs take D10's single-line `SMF18A` instead. ⚠️ **Never on a raw 12 V rail pin** — the TDK's over-voltage window (15.0–17.4 V) overlaps V<sub>BR</sub> min; the rail's clamp is D10. Price: DigiKey ~$0.63 each, indicative 2026-09-18. `SZSMS15T1G` is the AEC-Q101 version |
| **C5** | 1 | ST **`USBLC6-2SC6`**, SOT23-6L | ⬜ | ⬜ | `D409`, the USB D+ / D− ESD part at J401, placed by JLC: **3.5 pF max per line**, V<sub>RM</sub> 5.25 V. Its pin 5, the top of its steering diodes, ties to **3.3 V, not the USB VBUS**: D+ comes out of reset pulled up and would back-drive an unplugged VBUS to ~2.7 V. A general-purpose array (C2: ~300 pF) is tens of times too capacitive for a USB pair |
| **D1** | 4 | TI **`TPS4H160BQPWPRQ1`** | 📦 | **15.04** | ✅ Paid 09-10, **received 09-18** (4 @ $3.76, DK 296-44711-1-ND) — the breadboard's; JLC places the board set's. **2 fitted, all 8 channels used:** `U301` — channel 1 is `AUX12`, the current-limited feed to horn +, fan + and buzzer +, on whenever the logic runs · headlight LOW / HIGH / DRL · `U302` — tail running · turn L / R · channel 4 the **STOP lamp**, its input driven by the brake circuit's Q1 (G2) and never by firmware. 28-HTSSOP 0.65 mm with thermal pad — the hardest hand mount on the BOM (Notes; prototype adapter: X7). ⚠️ **40 V — 12 V rail ONLY.** ⚠️ **`CL` and `CS` each need a resistor** — without them the limit is the internal 8–14 A and there is no current sense (D12). TI's pin names are `VS` / `SEL` / `SEH` |
| **D2** | 7 | 20 kΩ pull-up, OUT → 12 V | ⬜ | ~1 | `R301`–`R306` + `R345` (STOP). Required for OFF-state open-load detect — one per lamp channel |
| **D3** | 10 | **`AO3400A`** (Alpha & Omega, genuine) | 📦 | **3.22** | ✅ Paid 09-10, **received 09-18** (10 @ $0.322, DK 785-1000-1-ND — `785-` = Alpha & Omega) — the breadboard's; JLC places the board set's. **5 fitted:** horn `Q301` · fan `Q302` · buzzer `Q303` · **Q2**, the run/off kill inverter `Q305` (all DRV) · boost `Q401` (BRAIN). Chosen for **48 mΩ guaranteed max at V<sub>GS</sub> 2.5 V** — clones lack that guaranteed figure. SOT-23 (prototype adapter: X6). ⚠️ No avalanche rating — the fan and the buzzer each take a flyback diode (D11) |
| **D5** | 1 | **Buzzer, 12 V** | ⬜ | ~2 | Only boost-mode feedback since D17. PWM transducer, not self-drive. Magnetic or piezo — its flyback (D11) is fitted either way |
| **D6** | 1 | **Cooling fan, 12 V** | ⬜ | ~8 | Thermostatic off serial temps |
| **D7** | ~15 | Screw terminals, 5 mm | ⬜ | ~8 | **Prototype only** (plan §9.6.1), where it consumes a whole board edge. The board set uses the pluggable terminals of X8, X9 and X11 |
| **D8** | — | 20 AWG bare copper | ⬜ | ~3 | **Prototype only** — 12 V bus reinforcement on perfboard, sized for the 2.62 A worst case (plan §3.2.3). The PCBs carry the rail in copper |
| **D9** | — | Gate bias-OFF and gate series resistors | ⬜ | ~5 | Every gate biased OFF (D14). 10 kΩ pull-downs ×4 (`R307`–`R309` horn / fan / buzzer · `R425` boost, the hard external pull-down) · 100 Ω gate series ×4 (`R310`–`R312` · `R424`) · 100 kΩ ×2 on HVIN: **`R110`, gate-to-SOURCE on `Q101` (D13)** — for a P-channel **high-side** switch, biasing OFF means V<sub>GS</sub> = 0, so the gate ties to its own **source**, not to ground — and `R113`, gate-to-ground on `Q105` (H2) |
| **D10** | 1 + 13 | **`SMBJ18A`** TVS, DO-214AA + **`SMF18A`** single-line TVS, SOD-123FL | ⬜ | ⬜ | The 12 V clamps, the 18 V grade throughout, placed by JLC. `D315` (`SMBJ18A`) clamps **the 12 V rail itself**, at the `TPS4H160B`s' `VS`. **No raw 12 V leaves the box:** horn +, fan + and buzzer + ride `AUX12`, U301's current-limited channel 1 (D1). Every 12 V lamp and load output takes its own `SMF18A`: `D308`–`D312`, `D319`–`D326`. Both parts: V<sub>RWM</sub> 18 V · V<sub>BR</sub> 20.0–22.1 V · V<sub>C</sub> 29.2 V — under the `AO3400A`'s 30 V and the `TPS4H160B`'s 40 V; the rules hold every part behind them to it (E20). ⛔ **The 18 V grade, not the 15 V grade (`SMBJ15A`):** the TDK's over-voltage window is 15.0–17.4 V and a 15 V part breaks down at 16.7–18.5 V, inside it — a converter fault would cook the clamp, which fails short and takes the 12 V rail, brake lamp included. The 18 V grade starts at 20.0 V, clear of the window |
| **D11** | 2 | **`SS14`**-class Schottky, 1 A / 40 V, SMA | ⬜ | ⬜ | Flyback across the fan (`D307`) and the buzzer (`D314`) at J305, cathode on `AUX12`, the loads' own + feed. Schottky is right here — G1's "not Schottky" concerns leakage into 3.3 V inputs, and these sit across a 12 V load. The horn is electronic and needs none (M7) |
| **D12** | — | DRV passives no other line covers — 0805 unless noted | ⬜ | ⬜ | `TPS4H160B` support: **1.00 kΩ 1 %** ×3 (`R319`, U301 `CL` → 2.0 A per channel · `R321` / `R322`, the `CS` sense resistors) · **2.0 kΩ 1 %** ×1 (`R320`, U302 `CL` → 1.0 A per channel) · **4.7 kΩ** ×11 (TI's series protection on the six firmware-driven `INx`, `DIAG_EN`, `SEL`, `SEH`, `FAULT1`, `FAULT2`) · 10 kΩ ×4 (a 10 k / 10 k divider from each `CS` node to its ADC pin, `R323` + `R339` · `R324` + `R340` — the `CS` pin rises to 4.5–6.5 V in any fault; the scale is 3.17 V/A at the `CS` node, **1.59 V/A at the ADC pin**) · 10 kΩ ×2 (`R349` / `R351`, the `FAULT1` / `FAULT2` pull-ups, TI's 10 k) · 10 kΩ ×1 (`R346`, U301 `IN1` → 3.3 V: `AUX12` is on whenever the logic runs) · 100 nF ×4 + 10 µF **/ 50 V** 1206 ×2 (ADC filters, `VS` decoupling — rated over D10's 29.2 V). The stop-lamp command: **27 kΩ** + 10 kΩ (`R347` / `R348`, Q1's drain → U302 `IN4`) and a **`BZT52B5V1`** zener, 2 % B grade (`D318`, holds `IN4` under its 7 V absolute). Sense: 47 kΩ + 10 kΩ + 100 nF (IN-15, 12 V → 2.11 V) · 100 kΩ ×1 (`BL` → `BL_SENSE`, the isolated copy firmware reads) · 100 kΩ + 180 kΩ (`R343` / `R344`, `ACC+` → 3.28 V at expander #2's `GPA0`: firmware sees whether the run/off kill's pull-up source arrived) · 1 kΩ ×2 (`R341` / `R342`, IN-05 / IN-06 series) |
| **E1** | 1 | TDK-Lambda **`CN150B110-12/CO`** | 📦 | **127.88** | ✅ Paid 09-10, **received 09-18** (Mouser 967-CN150B110-12/CO) **+ $31.97 tariff = $159.85.** `/CO` = factory conformal coating (EN 50155) on the internal board — cannot be retrofitted; +$14.78 over the uncoated $113.10, taken because the bike condensation-cycles. ⚠️ **160 V is a hard ceiling, transients included.** Conduction-cooled: **the baseplate bolts to a metal thermal interface** through its M3 holes (E16) — ⬜ check the mounting face is bare metal. ⬜ Its dissipation at the real ~21 % load is unmeasured (plan §3.2.3). ⚠️ **`CNT` is negative logic — open = OFF:** strap it to −Vin, and strap +S → +V, −S → −V, or the 12 V rail never comes up. TDK's required output network: E20, E21 |
| **E2** | 1 | Cincon **`EC7BW-110S05`** | 📦 | **61.66** | ✅ Ordered 09-10, **received 09-18**. 43–160 VDC → 5 V/4 A, 3 kV iso — the logic rail, kept separate so a lamp or horn fault cannot brown out the brain. ⚠️ Meets its EN 50155 EMC rating only *"WITH EXTERNAL FILTER"* — that filter is E7. Remote pin is positive logic, open = ON. Potted in a black-coated copper case, so no coating option as on E1. Hand-soldered on CONV (below): ⚠️ **its pins are 5.6 mm min with no maximum — trim them to ≤ 1.5 mm below the board before soldering** |
| **E3** | 1 | Littelfuse **`KLKD002.T`** 2 A fast | 📦 | **26.44** | ✅ Paid 09-10, **received 09-18** (DK 5666-KLKD002.T-ND; `.T` = packaging only). 2 A fast, 600 VAC/**600 VDC**, 50 kA DC. Fits at the module's B+ tap, in E4. ⬜ **Order 2–3 spares** — no substitute exists, and it does not discriminate against E11: a fault behind the 1 A link can open this one with it (Notes) |
| **E4** | 1+2 | Mersen **`FEB-11-11`** + 2× **`FSB1`** boots | 📦 | **58.49** | ✅ Paid 09-10 (DK 2378-FEB-11-11-ND) + 2× `FSB1` boots, ⬜ price. **Holder and both boots received 09-18.** Verified **600 VAC/DC**, 10×38, free-hanging inline, crimp, UL Recognized, 100 kA SCCR, 3 captive O-rings, 155 °C, tulip clips. ⚠️ **Boots are required** — the non-breakaway holder ships without them (*"non-breakaway units do not include insulator boots"*); the O-rings seal the fuse compartment, the boots seal the terminations. ⬜ **Also tape-wrap** — Mersen: *"due to varying wire insulation sizes it is suggested that tape wrap be utilized for best results."* On 12 AWG, tape or a light adhesive-shrink over the boot. ⚠️ **Terminal `11` = Cu crimp #8–#12 → 12 AWG minimum on a 0.62 A tap** — buy wire and crimp tool to match. Not `FEB-11-11-BA` ($128.19, utility breakaway, *"pole knockdown"*) |
| **E5** | 1 | TDK **`HAQ-10T`** heatsink | 📦 | **25.66** | ✅ Paid 09-10, **received 09-18** (DK 285-HAQ-10T-ND). **Not used in the stacked build — a spare.** 25.4 mm of fin on a 12.7 mm brick does not fit the stack, and a finned sink sealed inside a box only heats the trapped air (plan §3.2.3). The brick bolts to a metal thermal interface instead (E16), which bolts to the metal box's walls (X4). 7.5 °C/W is a free-air figure; 57.9 × 36.8 mm, the standard quarter-brick footprint, screw mount — it suits a free-standing carrier |
| **E6** | 2 | Schurter **`FAC 0031.3803`** holders | 📦 | **9.78** | ✅ Paid 09-10, **received 09-18** (2 @ $4.89, DK 486-1257-ND). **Not used in the stacked build — both are spares.** It is a **47.5 mm-tall VERTICAL** holder (14.9 × 14.9 mm on the board) and cannot fit the stack; it also ships without its cap (`0031.1663`). `F201` sits in `FH201`, two PCB fuse clips (E22). 600 VAC/VDC (UL) — it suits a free-standing carrier |
| **E7** | 2 | Würth **`7448022010`** choke | 📦 | **17.84** | ✅ Paid 09-10, **received 09-18** (2 @ $8.92, DK 732-13589-ND). ⚠️ **Two, not one:** TDK requires a choke per supply input, before the bulk cap; Cincon's EC7BW meets EN 50155 *"WITH EXTERNAL FILTER"* only. 10 mH, 2 A @ 70 °C, AEC-Q200, at 29% of rating. **A four-terminal part** (windings 1–4 and 2–3): each converter's −Vin is its own net, joined to ground only through its winding. **22.0 mm tall** × 18.0 × 14.0 mm — the tallest part on HVIN. `L101` / `L102`, hand-soldered on HVIN (below) |
| **E8** | 2 | Chemi-Con **`EKXJ221ELL221MM25S`** (KXJ, 220 µF / **220 V**) | 📦 | **8.02** | ✅ Paid 09-10, **received 09-18** (2 @ $4.01, DK 565-EKXJ221ELL221MM25S-ND — *"CAP ALUM 220UF 20% 220V RADIAL"*). Meets the plan's ≥220 µF / 200 V requirement with margin; same `MM25S` 18 × 25 mm size as the 200 V `EKXJ201…`. ⚠️ **Two are needed, different nodes** — TDK's EMC bulk at DC-DC #1's input, hold-up behind the diode at #2; **440 µF total downstream of D13, which is what the SOA figure turns on.** `C201` / `C202` lie down, bonded, on CONV's underside; the board set's pair is ordered loose with the boards (below) |
| **E9** | 4 | **Vishay `VY2472M49Y5US6`** Y2 4700 pF | ✅ | ~2 | `C203`–`C206`, ordered loose with the boards as the `…TV7` reel part (kinked 7.5 mm leads) and bent flat by the owner (below). Verified from Vishay's datasheet: 4700 pF ±20% (M), **12.5 mm body**, Y5U. *"Class X1, 440 VAC, Class Y2, 300 VAC"*, **IEC 60384-14**, and an explicit **`1000 VDC`** rating — ours is a DC application (84 V rail → baseplate), ~12× margin. Test 2600 VAC/2 s. IEC/UL/DIN-VDE/CSA/CQC, UL 94 V-0, IR ≥ 10 000 MΩ. **−40…+125 °C — needed: they sit at the converters' input terminals beside a baseplate allowed to reach 100 °C.** Why Y-class (not the voltage): they bridge +Vin / −Vin to the **baseplate** and the metal it bolts to, so a short-failure puts the 84 V rail on that metal; IEC 60384-14 certifies a safety cap against exactly that, and the baseplate's single-point tie to ground (E21) turns such a short into a blown tap fuse. A plain 2 kV ceramic is the wrong part. ⚠️ The trailing `###` selects packaging (DK `…TV0`, Mouser `…TV7`). ⚠️ **They lie FLAT** (5.0 mm; upright they are 15.5–16.5 mm) — 4 × 12.5 mm discs is real board area on CONV |
| **E10** | 1 | **`1N4007`**, DO-41 | ~ | ~0.15 | The breadboard's hold-up blocking diode. On the board set `D201` is the SMA **`M7`** — the SMA 1N4007, 1000 V / 1 A, 30 A surge — placed by JLC; it carries ≤ 0.62 A behind F201 |
| **E11** | 1 | **Schurter `0001.2504`** 1 A T-lag | 📦 | **4.93** | ✅ Paid 09-10, **received 09-18** (5 @ $0.986, DK 486-1782-ND). Listing: *"1A 250VAC 300VDC 5X20"*. **1 fitted** — `F201` on DC-DC #2 per Cincon's datasheet, **clipped into `FH201` (E22)**; the board set's is ordered loose with the boards (below). The five in hand are the breadboard's and spares — bring-up will blow one. ⚠️ **Order SPT by part number** — the glass `FST` and ceramic `SP` are the same size with **no DC rating at all** |
| **E12** | 1 | **`SMCJ90A`** TVS | ✅ | ~0.47 | ⚠️ Not `SMBJ90A` / `SMBJ100A` / `5KP90A`, and the 1.5 kW part, not the 600 W `SMCJ90A-L`. **`D101`, B+ → ground at J101** — the only TVS on an 84 V wire, placed by JLC. ⛔ **No TVS on the key tap (`KSW`):** it is a ≥100 kΩ resistive input, and a TVS there that failed short would blow the key fuse and cut the FarDriver KEY. V<sub>R</sub> 90 V · V<sub>BR</sub> 100–111 V · **146 V @ 10.3 A** (I<sub>PPM</sub> at 25 °C; ~7.4 A at 60 °C) — under the converters' 160 V ceiling |
| **E13** | 2 | IXYS **`IXTA26P20P-TRL`** TO-263 (D2PAK) + zener | ⬜ | ~6 | **1 fitted — `Q101`, D13's module power switch, placed by JLC — + 1 spare.** 4.83 mm tall. ⚠️ **The tab is the drain, at 84 V, soldered to board copper:** keep clearance to every other net, and no metal under it. The `IXTP26P20P` (TO-220AB) is the same die on the same datasheet and SOA — the hand-soldered fallback. Chosen on **SOA**: datasheet **DS99913D** publishes **10 ms / 100 ms / DC** lines at T<sub>C</sub> 70 °C, and the ~50 ms soft start's 114 W clears even the DC line (~191 W at 84 V). ⚠️ Use DS99913D, not the 2007 "Preliminary" sheet some distributors serve. Not `FQP12P20` (no SOA plot). Its 15 V gate zener is E17 |
| **E14** | 1 | IN-12 KEY-sense divider: 165 kΩ 1206 ×2 + 10 kΩ + 100 nF | ⬜ | — | `R107` / `R108` / `R109` / `C109` on HVIN, fed from the key-switch output (`KSW`, J101 pin 6). 330 k / 10 k: 84 V → 2.47 V; the series pair shares the voltage (40.8 V each). `KEY_SENSE` then crosses three boards to `GPIO1`, through 1 kΩ with 100 nF at the pin (A7). Key state only — never a battery gauge |
| **E16** | 1 | Four-board PCB set + the brick's **metal** plate + stack hardware | ⬜ | ⬜ | HVIN · CONV · DRV · BRAIN (plan §9.2), **4-layer**, JLC's standard 1.6 mm stack; outlines follow M18. ⚠️ **The brick bolts to metal, never proto board** — it is conduction-cooled: a 3.0 mm alloy plate above CONV, bolted down onto U201's M3 baseplate threads with **countersunk ISO 10642 M3** screws whose heads sink into the plate (a pan or button head stands proud into the gap under DRV). The plate is also the barrier between the 84 V and logic halves, and bolts to the box's walls (X4); its shape is decided with the enclosure, after M18. M3 standoffs for the stack. ⬜ No part chosen |
| **E17** | 2 | Zeners, SOD-123, the 2 % B grade: **`BZT52B15`** ×1 + **`BZT52B10`** ×1 | ⬜ | ⬜ | `D102`, 15 V — `Q101`'s gate clamp, inside its ±20 V V<sub>GS</sub> (E13) · `D106`, 10 V — `Q105`'s gate clamp, inside the `BSS127`'s ±20 V (H2). Neither conducts in normal running (the nodes sit at −13.1 V and 7.6 V). ⛔ **Not the 5 % C grade:** a `BZT52C15` can clamp at 13.8 V, 0.7 V above Q101's running V<sub>GS</sub>. JLC places both |
| **E18** | 1 | TDK **`CGA9N1C0G2J683JT0Y0S`** — 68 nF C0G, 630 V, 2220 | ⬜ | ⬜ | `C105`, the Miller capacitor gate-to-**drain** on `Q101` — it sets the ~50 ms ramp. JLC places it. ⚠️ **Check the voltage line:** with the switch off it holds the full pack, and 146 V at E12's clamp — hence ≥250 V. ⛔ **Not X7R** — it loses about half its value at that bias, exactly where SOA stress peaks. ⚠️ **Layout: keep it away from the mounting holes and the board edges** — a 2220 cracks under board flex, and shorted it holds Q101 on with the key off |
| **E19** | — | D13 gate-network passives | ⬜ | ⬜ | **4.7 µF, 50 V X7R**, 1206 ×1 (`C107`, gate-to-source: it divides down the dV/dt that E18 couples into the gate when the XT90-S is mated with the key off — 84 V × 68 n / (68 n + 4.7 µ) ≈ 1.2 V, under the 2.0 V minimum threshold, and −1.64 … −1.98 V at the tolerance, temperature and over-voltage corners. So it must keep its value: X7R at 50 V, where a Y5V / 25 V part loses most of it under bias and cold) · **270 kΩ 1206 ×2** (`R101A` / `R101B` = **540 kΩ**, the turn-on pull-down: V<sub>GS</sub> −13.1 V at 84 V, −9.4 V at 60 V) · **499 kΩ 1206 ×2** (`R112A` / `R112B`, key-switch output → `Q105`'s gate) · 100 nF ×1 (`C108`, key-contact bounce). Each series pair shares the voltage — 1206 for the rating. The 100 kΩ bias resistors are in D9 |
| **E20** | 1 | JIERR **`PA35V680M10x15`** — 680 µF 35 V polymer, 16 mΩ, 10 × 15 mm radial | ⬜ | ⬜ | `C207` — **TDK requires it** across the 12 V output *"for stable operation"* (instruction manual Table 6-1: 12 / 15 V models, 25 V 680 µF solid). **35 V, not 25:** D10 lets the 12 V rail reach 29.2 V. It lies down beside the brick, **10.5 mm tall** under the brick's 12.7 mm; ordered loose with the boards and bent over by the owner (below) |
| **E21** | — | CONV passives no other line covers | ⬜ | ⬜ | TDK's output network: 2.2 µF **/ 50 V** 1206 ×1 (`C208` — rated over D10's 29.2 V) · **22 nF / 250 V** 1206 ×2 (`C209` / `C210`, +V and −V → baseplate — 250 V so they survive the baseplate lifted to the 84 V rail) · Cincon's output: 10 µF / 16 V 1206 + 100 nF (`C211` / `C212`) · `R211`, the single-point tie baseplate → ground, is a **copper net-tie ≥2 mm wide, not a part** — a shorted Y2 must blow E3, and a 1206 jumper could open first against ~200 A prospective, leaving the plate floating at 84 V |
| **E22** | 2 | Littelfuse **`01110501Z`** 5 × 20 mm PCB fuse clips | ⬜ | ⬜ | `FH201` — **both clips in one footprint**, their 17.8 mm spacing fixed in copper; they hold `F201` (E11) on CONV. 5 mm clip with fuse stop, 10 A. Ordered loose with the boards and soldered by the owner (below). The DC interrupting duty is the fuse's, not the clip's. ⬜ Seated height (7.1 mm catalogue body) to confirm on a real part. E6 cannot do this job in the stack |
| **F** | — | Display power switch | ⏸️ | — | **Parked with D11/D19 — not in this order** |
| **G1** | 10 | **1N4148** small-signal diode | ⬜ | ~1 | Brake circuit steering diodes D1L/R · D2L/R · D3L/R (`D301`–`D306` on DRV) + 4 spare ([`brake-circuit.md`](https://github.com/bnich/Revv1-FS-72v-Conversion/blob/main/docs/brake-circuit.md) §3). ⚠️ Silicon, **not Schottky** — a Schottky's reverse leakage reaches the 3.3 V inputs when hot |
| **G2** | 2 | **`AO3407A`** P-channel MOSFET, −30 V, **±20 V gate** | ⬜ | ~1 | Q1 (`Q304` on DRV), the hardware stop-lamp command (plan D23), + 1 spare: it drives U302's channel-4 input through D12's 27 k / 10 k divider, so the STOP lamp gets a current limit, open-load detection and `FAULT2` with no firmware in the switching path. SOT-23 (prototype adapter: X6). A ±12 V-gate part is marginal on the 12 V rail |
| **G3** | 4 | 10 kΩ ×3 + **1 kΩ** ×1, ¼ W | ⬜ | ~0.5 | R1 (`R313`) — Q1's gate pull-up to +12 V, which is also the lever's wetting current · R3L / R3R (`R317` / `R318`) — the IN-05/06 pull-ups to 3.3 V · R4 (`R314`, **1 kΩ**) — the run/off toggle's pull-up from `ACC+` (§2.1) and its wetting current, 5.1 mA; on DRV so an open bar wire reads OFF |
| **G6** | 2 | 100 Ω · 100 kΩ ¼ W | ⬜ | ~0.5 | R5 / R6 (`R315` / `R316`) — gate series and gate-to-ground for **Q2**, the run/off toggle's inverter (§2.1). Q2 itself is an `AO3400A` from **D3** |
| **G4** | 1 | 100 nF ceramic | ⬜ | ~0.5 | C1 — `BL` ↔ B− at the controller (EMI). A harness part, not a board part |
| **G5** | 1 | 1 A automotive mini-blade (ATM) fuse + inline holder | ⬜ | ~2 | F1 — the brake-lamp feed; the STOP lamp draws 0.12 A (M5). A harness part |
| **H2** | 2 | Infineon **`BSS127`** N-channel, 600 V, **enhancement-mode**, SOT-23 | ⬜ | ~1.5 | **1 fitted + 1 spare.** `Q105`, the level shifter that turns D13's `Q101` on: key on → its gate sits at 7.6 V (5.5 V at the 60 V LVC) → it pulls the 540 kΩ string (E19) to ground. Key off → 0 V → no DC path off `Q101`'s gate, and zero quiescent drain. V<sub>GS(th)</sub> 1.4–2.6 V. ⛔ **Not `BSS126`** — that same-family sibling is **depletion-mode, ON at V<sub>GS</sub> = 0**: it would hold the module on with the key off. ⚠️ A 30 V part (`AO3400A`) will not stand off the 84 V node. Its gate parts: D9, E17, E19. SOT-23 (prototype adapter: X6) |
| **X6** | 20 | Cermant **SOT23-6 / SC70-6 → DIP** breakout boards | 📦 | **8.49** | ✅ Paid 09-10, received (Amazon). **Prototype only** — JLC places every SOT-23 and SC-74 part on the board set. Use the **SOT23-6 side (0.95 mm)**, never the SC70-6 side (0.65 mm). **C2** and **C4** fit it directly — Nexperia: *"SOT457 (also referred to as SOT23-6)"*. A **3-lead SOT-23** (D3 `AO3400A`, G2 `AO3407A`, H2 `BSS127`) sits on **pads 1, 3 and 5**: lead 1 → pad 1, lead 2 → pad 3, lead 3 → pad 5 |
| **X7** | 4 | **HTSSOP-28 exposed-pad → DIP-28 adapters** for D1 | ⬜ | ~30 | **Prototype only** — JLC places the `TPS4H160B` on DRV, pad to copper. Chip Quik / Proto Advantage **`2200235`** (*"TSSOP-28-Exp-Pad to DIP-28, 0.65 mm"* — the **Exp-Pad** variant is the one that matters); ⬜ **confirm it routes the pad to a pin or via array, or the thermal path is lost.** **Buy 4** — a failed mount can take the adapter. Also on Amazon (`B00JU3IXLQ`). ⬜ **Price unverified** — ~$7.50 each assumed |
| **X1** | 2 | **Bar-to-box cable + connectors** | ⬜ | ~15 | ~10 lines per side. The module ends are X8's screw plugs: 9-way left pod (`J402`), 6-way right pod (`J403`). ⚠️ Not the same as X5's enclosure glands |
| **X2** | — | FR4 **plated** proto board + stripboard | ⬜ | ~15 | **Prototype only.** ⚠️ Not phenolic/FR2 — pads lift under vibration |
| **X4** | — | Enclosure | ⬜ | ⬜ | ⬜ **An all-metal box, CNC-machined by JLCCNC from this project's models.** The model follows M18; until it exists, its wall, floor and lid thicknesses are allowances. The brick's plate bolts to the box walls (E16). The box is bonded to ground and **insulated from the frame** (gap pad, nylon hardware), or the frame becomes a second B− return. ⚠️ **A 0.5 mm insulating liner (Formex GK-17 class) on the floor under HVIN**, whose underside carries 84 V pins: without it, one bent tail or stray strand is a pack short. It kills a PCB antenna, which is why A4 is the `-1U`. ⚠️ **The fit does not close yet** (README, *Where the design stands*): size, cable exit and partitioning are decided with M18 |
| **X5** | — | Glands, strain relief, coating | ⬜ | ~20 | Where the harness bundles leave the metal box (X4). ⬜ The cable exit is decided with M18 and the enclosure |
| **X8** | 8 | **Locking pluggable screw terminals, 3.81 mm**: Kangnex `WJ15EDGRM-3.81` right-angle headers (JLC places them) + `WJ15EDGKM-3.81` screw plugs (ordered loose) | ⬜ | ⬜ | Every harness bundle but the pack's, the levers' and the brake/kill's (Notes: *harness terminals*). DRV: 2-way ×1 (horn) · 4-way ×3 (headlight · front turn · fan + buzzer) · 5-way ×1 (tail + rear signals). BRAIN: 6-way ×1 (right pod) · 7-way ×1 (FarDriver serial + boost + the boost button) · 9-way ×1 (left pod). ⏸️ `J310` (one-line in, 2-way) and `J405` (display, 9-way) are parked: footprint only, **no header fitted**, no plug ordered. 300 V / 8 A UL, 160 V / 7 A IEC, 28–16 AWG (1.5 mm²), strip 6–7 mm, 0.2 N·m. The header stands **7.25 mm** (9.20 deep), pins 3.7 mm max below the body; a mated plug stands 9.6 mm past the header's face. ⚠️ **The three 4-way plugs fit each other's headers** (J301 / J303 / J305): label every plug, and check every lamp after service |
| **X9** | 2 | **Locking pluggable screw terminals, 5.08 mm**: Kangnex `WJ2EDGRM-5.08` headers (JLC places them) + `WJ2EDGKM-5.08` plugs (ordered loose) | ⬜ | ⬜ | DRV: `J306`, the brake levers (4-way: `LEVER_L`, GND, `LEVER_R`, GND) · `J309`, the FarDriver brake/kill (3-way: 1 `BL` · 2 GND · 3 `ACC+`) — `BL`'s only neighbour is ground, so a strand across them cuts the motor: fail-safe. Alone in their family, so no lamp or pod plug seats in either. 300 V / 10 A UL, 320 V / 15 A IEC, 24–12 AWG (2.5 mm²), strip 7–8 mm, 0.4 N·m. The header stands **8.30 mm** (12.20 deep), pins 4.2 mm max below the body; a mated plug stands 8.8 mm past its face. ⚠️ **M3** (lever type and wire count) can change the lever plug's size |
| **X10** | — | **2.54 mm inter-board headers** + the service and spare-input headers | ⬜ | ⬜ | ⬜ **Family unchosen — and the mated pair SETS each board gap**, so it is chosen before layout (plan M19). Four interfaces, each one crossing between neighbouring boards — the lower half on top of the lower board, the upper half **under** the upper board: HV-LINK `J104` (HVIN) ↔ `J201` (CONV), 11 positions at 5.08 mm effective (a 2.54 mm header with alternate pins skipped, for 84 V creepage), each converter pair on two contacts · PWR-UP `J202` (CONV) ↔ `J311` (DRV), 1 × 11, four contacts of 12 V for the 2.62 A · PWR-BRAIN `J307` (DRV) ↔ `J407` (BRAIN), 1 × 5: 5 V, `KEY_SENSE` and ground — BRAIN takes no 12 V · STACK `J308` (DRV) ↔ `J406` (BRAIN), 2 × 25, every signal facing a ground. ⚠️ Plan §9.6.2's vibration rule applies — machined contacts and mechanical retention, never dual-wipe. Plus `J408`, BRAIN's internal 1 × 6 right-angle service header, placed by JLC: `EN`, `IO0`, U0TXD (through 470 Ω), U0RXD, —, ground — **no supply**: the USB-serial adapter powers itself. And `J409`, **not fitted**: a 2 × 8 vertical header footprint for expander #2's 13 spare inputs, each class-A protected on the board (a dry contact to ground or 3.3 V logic, nothing above 5 V); when one is wanted, fit an XFCN `PZ254V-12-16P` |
| **X11** | 1 | **Locking pluggable screw terminal, 7.62 mm, 6-way**: Kefa `KF2EDGRM-7.62-6P` header (JLC places it) + `KF2EDGKM-7.62-6P` plug (ordered loose) | ⬜ | ⬜ | `J101` on HVIN — **the only connector that carries pack voltage**: 1 B+ · 2 empty · 3 B− · 4 B− · 5 empty · 6 `KSW`, the key switch's output. The empty positions put a pitch of air around B+ and around the key tap, so no strand can pull KEY down. Two B− conductors, so one open return cannot push the input current through a signal ground. The only 7.62 mm terminal: no other plug seats in it, and its plug seats nowhere else. Wired mirror-image, the plug only swaps B+ and `KSW` — the module is then always on; both B− stay B−. 300 V / 10 A UL, 630 V / 15 A IEC, 24–12 AWG (2.5 mm²), strip 7–8 mm, 0.4 N·m. The header stands **8.60 mm** (12.15 deep), pins 4.3 mm max below the body; a mated plug stands 9.65 mm past its face. The `KLKD002` (E3) in its holder (E4) is upstream, in the harness |

**Not on this BOM:** `BSS138` for the boost line — an `AO3400A` from D3 covers it (30 V is ~2.5× the 3.3–12 V pull-up plan §7.1 states; 48 mΩ vs 6 Ω max, 5.7 A vs 220 mA; one FET type across all low-side channels — ⚠️ but meter the wire first, Notes) · status screen (plan D17) · 4 momentary buttons (plan D3) · `TPIC6B595` · a brake-sense divider — the brake circuit feeds IN-05/06 through G1's diodes with G3's 10 kΩ pull-ups · anything for a start latch — the key switch feeds the FarDriver KEY wire directly (plan §3.2.5a).

---

## JLC assembly — the custom boards

The custom boards are **4-layer** (JLC's standard 1.6 mm stack) and assembled by JLC from **LCSC
parts, JLC Basic first**. The LCSC part does not have to be the part bought for the breadboard
above. Every LCSC number lives in [`tools/netlist.py`](../tools/netlist.py), and nowhere else:
- parts bought by value: `_R_LCSC` and `_C_LCSC`;
- parts bought by part number: `_FAB_BY_MPN`, and `_LOOSE_BY_MPN` for the loose parts below;
- the harness terminals' headers and plugs: `_TERMINALS`; other connectors: `_FAB_CONN`.

A resistor's or capacitor's working voltage is its chosen part's, and the rules check it against the
circuit. `python3 -m tools.jlc_bom --out bom.csv` writes the file JLC's order takes. Run without
`--out`, it also lists what is ordered loose and what is hand-soldered, with every LCSC code and
count — order from that list, not from this page.

**Ordered loose with the boards, fitted by you.** JLC inserts through-hole parts upright; the
height budget counts these lying down, and the fuse clips in.

| Ref | Part | How it is fitted |
|---|---|---|
| every fitted harness header | its screw plug (X8, X9, X11) | wired outside the box, pushed in from the board edge, screwed to the header's flanges |
| C201, C202 | Chemi-Con EKXJ221ELL221MM25S, 220 µF 220 V (E8) | bent over and bonded, lying on CONV's underside |
| C203–C206 | Vishay VY2472M49Y5US6 Y2, the reel part with kinked 7.5 mm leads (E9) | bent flat |
| C207 | JIERR PA35V680M10x15, 680 µF 35 V polymer (E20) | bent over, lying beside the brick |
| F201 | Schurter 0001.2504, 1 A T-lag 5 × 20 (E11) | clipped into FH201 |
| FH201 | Littelfuse 01110501Z, two clips (E22) | both soldered into FH201's one footprint |

**What JLC cannot place, which you buy and solder:**

| Ref | Part | Why |
|---|---|---|
| U201 | TDK CN150B110-12/CO (E1, in hand) | nothing on LCSC takes 43–160 V in and gives 12 V at ≥ 2.62 A with stock |
| U202 | Cincon EC7BW-110S05 (E2, in hand) | LCSC's nearest 43–160 V 5 V module numbers its pins differently and states its isolation two ways. ⚠️ Trim its pins first (E2) |
| L101, L102 | Würth 7448022010 (E7, in hand) | LCSC has none; its nearest has 2.4× the DCR and no voltage rating |

Their land patterns are drawn from the makers' drawings (`tools/drawn_footprints.py`).

**Not fitted** — footprints only: `U406` + `C436` and `R442` (A7) · `D317`, `D406` (C4) · the
`J310` and `J405` headers (X8) · `J409` (X10). The generated EasyEDA project marks these, and every
part you fit yourself, `Add into BOM: no`: EasyEDA's BOM ignores its own DNP flag.

⬜ **The inter-board connectors** (J104, J201, J202, J311, J307, J407, J308, J406) wait on the
connector family, which is an open decision. `tools.jlc_bom` lists them as not yet chosen.

## Order history

| Date | Order | Lines | Paid | State |
|---|---|---|---|---|
| pre-09 | **Owned stock** | **A1** DevKitC-1 `WROOM-1-N8` (v1.0) · **B1** `SN65HVD230` breakouts ×3 · **B2** 120 Ω (on breakout) | — | 📦 In hand. ⚠️ B1's vendor/PN never recorded — 120 Ω fitted, no `Rs` pin |
| pre-09 | ElectroCookie `B07ZYNWJ1S` / `B082KYCJXJ` proto boards | — | — | 📦 Spares — generic FR4 (**X2**) is used instead |
| **09-10** | **Amazon** | **X6** Cermant SOT23-6 / SC70-6 → DIP ×20 | **$8.49** <br>+ $0.57 tax = **$9.06** | 📦 Received |
| **09-10** | **DigiKey — 10 lines** (SO 101547984) | **C2** ×20 · **C3** ×4 · **D1** ×4 · **D3** ×10 · **E2** · **E3** · **E5** · **E6** ×2 · **E7** ×2 · **E8** ×2 | **$182.96** <br>+ $8.49 ship · $14.68 tax · **$26.09 tariff** = **$232.22** | 📦 **Received 09-18** |
| **09-10** | **Mouser** | **E1** TDK `CN150B110-12/CO` | **$127.88** <br>+ **$31.97 tariff** | 📦 **Received 09-18**. 25% tariff |
| **09-10** | **DigiKey — fuse pair** | **E11** `0001.2504` ×5 · **E4** `FEB-11-11` | **$63.42** | 📦 **Received 09-18** |
| **09-10** | **Boots** | **E4** `FSB1` ×2 | ⬜ | 📦 **Received 09-18**. ⚠️ Utility channel, not DigiKey |
| **09-11 / 09-12** | — | **A3** bar-mount switch sets ×2 | ⬜ | 📦 **Both in hand** — left 09-11 (harnessed, working), right 09-12 (measured, rewired) |

**Committed: $382.75** (+$31.97 tariff on the Mouser line). ⬜ Prices still to log: **E4**'s boots, **A3**.

## Notes

- **Reasoning lives in [`plan.md`](plan.md)**, not here — §3.2 power/D13 · §4 interface classes
  and the wetting-current rule · §6 outputs · §9.2 the four boards · §9.5 sourcing · §8 the decision
  table. ⚠️ **Change a part here first**, then the plan section that carries its reasoning.
- ⬜ **Not yet checked against what is already on the shelf** — the BOM was built from what the docs
  specify. Likely candidates: A2 socket strips · D7 screw terminals · the generic passives (A7, B3, C1,
  D2, D9, D12, E14, E19, E21, G1, G3, G4). A shelf pass cuts both cost and price-hunting.
- ⚠️ **D1 is the hardest hand mount on the BOM.** `TPS4H160BQPWPRQ1` is **28-HTSSOP, 0.65 mm pitch,
  with an exposed thermal pad** carrying the heat of its four current-limited channels, and **the pad
  must reach real copper or the per-channel current limiting and thermal shutdown will not behave as
  specified.** On the board set JLC places it, and the layout ties the pad to ground copper. For the
  breadboard, solder a spare to an X7 adapter first and prove the technique. A5's PowerPAD is the
  same soldering class. *(Plan §9.1 covers what may and may not sit on a breadboard.)*
- **Specs come from the manufacturer PDF (`pdftotext`), never a web-fetch summary** — the summary got C2's
  polarity and pitch wrong (plan §12).
- ⛔ **A TVS follows its line's IDLE voltage, not the logic level behind it.** C2 (5 V) on a 12 V
  line or a brake-lever node is a dead short that nothing in a schematic shows; C4 (15 V) on the raw
  12 V rail sits inside the TDK's over-voltage window. Three jobs: C2 ≤5 V lines · C4 the lever
  nodes, `BL` / `ACC+` and the boost output · D10 the 12 V lamp and load outputs (`SMF18A`) and the
  rail itself (`SMBJ18A`) (plan §6.2.4). Every TVS in the netlist states its clamping voltage, and
  the rules hold each part it protects to it.
- ⚠️ **Harness terminals come in three families, by job** — 7.62 mm (X11) pack voltage only ·
  5.08 mm (X9) the levers and the brake/kill · 3.81 mm (X8) the rest — so a plug of one job cannot
  seat in a header of another. Within a family a smaller plug seats, offset, in a larger header, so
  every safety-relevant terminal (84 V, the levers, `BL` / `ACC+`, `RUN`, boost) has a size nothing
  else in its family shares: with every plug seated, a mis-plug leaves a plug in the hand. One plug
  per harness bundle, nothing to crimp, no housing to match: wire it outside the box, push it in from
  the board edge, and two screws lock it to the header's flanges.
- ⚠️ **Meter the boost wire before connecting a 30 V FET to it.** §7.1's *"3.3–12 V pull-up inside the
  controller"* is unsourced, and the candidate wire (`CruisePin` PIN17) has never been metered.
  `BOOST_OUT` carries C4's 15 V array (V<sub>BR</sub> 16.7 V min), so **the pull-up must measure
  ≤ 15 V** for the `AO3400A` path; above that, the array would conduct continuously — use the
  **PC817** output plan §6 offers for the boost line instead. ⛔ **The same 30-pin harness carries
  pink `60VC` at 72–84 V** — landing on it destroys any low-voltage FET, so this is a measurement,
  not a part choice. Keep `60VC` capped and taped back on its own (pull its pin from the plug if
  possible): shorted onto a serial wire, it burns `R436` / `R443`. Procedure:
  [`inputs-bench-session.md`](inputs-bench-session.md) §7, C1.5.
- ⛔ **Fuse and holder part numbers look interchangeable and are not.** For anything fuse-related, read
  the datasheet's **fuse-type line, the dimension drawing and the ordering-table footnotes** before the
  price. Four that looked right: Schurter **`FST`** (same 5×20 size, glass, **no DC rating**) · Schurter
  **`SP`** (ceramic, fast-acting, high-breaking — **no DC rating anywhere**) · Littelfuse
  **`01500330HXU`** (looked like E3's holder; actually **6.3×32 mm 3AG**, **IP40**, *"certifications do
  not apply"*) · Schurter **`FAC 0031.3803`** (E6 — electrically right, **47.5 mm tall**, sold without
  its cap).
- ⚠️ **Tariffs are in no figure here.** Mouser added **$31.97 (25%)** on E1's $127.88, and DigiKey order
  101547984 added **$26.09**. Treat every all-in number as **ex-duty**.
- **Holders and elements are one purchase:** E3 + E4 (inline, B+ tap) and E11 + E22 (clips on CONV,
  DC-DC #2). Neither works half-bought — including when reordering spares.
- ⚠️ **E3 has no spares and no substitute.** A 2 A fast-blow at **600 VDC / 50 kA DC** is a narrow part —
  Schurter has **no** fast-acting 5×20 with any DC rating. A bring-up wiring error will pop it, a fault
  behind E11 can take it too (melting I²t 2.4 A²s against the 1 A T-lag's 1.1 A²s — too close to
  discriminate), and a hardware-store fuse is not a stand-in on an 84 V pack. ⬜ **Order 2–3 spares.**
- ⬜ **Keep the TDK CN-B datasheet and instruction manual with the other vendor documents** under
  `fardriver-nd72450-reference/reference/` — cited, never committed. Sources:
  `sg.lambda.tdk.com/wp-content/uploads/cn-b_e.pdf` and
  `product.tdk.com/…/instruction_manual/cn50-150b110_apl.pdf`. TDK states its derating applies
  unchanged to the `/CO` option.
- **Not in this BOM:** the bike's own parts — wheel, tyres, controller, battery, Class T fuse and block,
  torque arms. Those are in the build sheet's order tracker.
- **Totals are summed from the rows:** committed = the 14 paid rows; estimated = the 25 unpaid rows
  that carry a `~` price, with A3 at the low end of its range. The 21 rows priced ⬜ are in neither, so
  the all-in figure is a floor.
- **D23 — the brake light is hardware.** The STOP lamp hangs on U302's channel 4, whose input only the
  brake circuit's Q1 (G2) drives: no firmware in the path, and the channel still gives the lamp a
  current limit, open-load detection and `FAULT2`. IN-05/06 take G3's 10 kΩ pull-ups rather than a C1
  network. Only milliamps flow through the lever switch, so any lever type that passes **M3** works
  ([`brake-circuit.md`](https://github.com/bnich/Revv1-FS-72v-Conversion/blob/main/docs/brake-circuit.md) §4).
  ⚠️ The lamp is fed from the module's 12 V rail: an unpowered module means a dark brake lamp. The
  motor cut (`BL`) needs nothing from the module.
